activities:
  - prefix: 'a'
    range: [1,8]
  - a
  - b

partitions:
  - formula:
      declare:
        - chainresponse(a_1,a_2)
    label: pos_1

    trace_length:
      numpy.random: normal
      kwargs:
        loc: 30.0
        scale: 1.0

    num_traces: 10

  - formula:
      declare:
        - chainresponse(a_1,a_4)
    label: pos_2

    trace_length:
      numpy.random: normal
      kwargs:
        loc: 10.0
        scale: 1.0

    num_traces: 10


  - formula:
      declare:
        - chainresponse(a_1,a_3)

    label: pos_3

    trace_length:
      numpy.random: normal
      kwargs:
        loc: 20.0
        scale: 1.0

    num_traces: 10
