activities:
  - prefix: 'a'
    range: [1,8]
  - a
  - b

partitions:
  - formula:
      declare:
        - chainresponse(a_1,a_8)
      ltlf:
        - G(a -> F(b))
    label: "pos_2"

    trace_length:
      numpy.random: normal
      kwargs:
        loc: 5.0
        scale: 1.0

    num_traces: 10

  - formula:
      declare:
        - chainresponse(a_3,a_4)

    label: "pos_1"

    trace_length:
      numpy.random: normal
      kwargs:
        loc: 3.0
        scale: 1.0

    num_traces: 10
